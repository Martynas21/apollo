# Rust build artifacts
/target

# Environment variables (secrets) - copy .env.example to .env
.env

# Local SQLite database files (per-user OAuth token storage)
*.db
*.db-journal

# Editor/OS cruft
.DS_Store
